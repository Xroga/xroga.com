import {EditorialPage} from '@/components/seo/EditorialPage';import {buildMetadata} from '@/lib/seo';const title='Security and control in the Xroga build workflow';const description='Understand Xroga security boundaries for repository access, provider credentials, server enforcement, validation, approvals, and truthful release status.';export const metadata=buildMetadata({title,description,path:'/security',keywords:['AI coding security','secure AI app builder']});export default function Page(){return <EditorialPage path="/security" title={title} description={description} eyebrow="Trust through explicit boundaries" intro="Security is not a badge added after generation. It is the set of controls that determines what Xroga can read, change, execute, and publish—and what evidence the product returns." breadcrumbs={[{label:'Security',href:'/security'}]} related={['/learn/production-readiness-checklist','/github-ai-coding-agent','/docs']} sections={[{heading:'Authorised accounts',paragraphs:['Repository and provider operations use the permissions the connected user grants. Xroga does not claim access beyond those scopes and should report provider or policy blocks directly.']},{heading:'Protected credentials',paragraphs:['Secret values belong in protected server or provider environments. Interfaces may show configuration names and readiness, but must not return secret values to the browser or include them in generated code.']},{heading:'Repository write safety',paragraphs:['The active project context—repository, branch, and project scope—must match the target at the write boundary. Branch protections, organisation policy, concurrency controls, and review requirements remain authoritative.']},{heading:'Validation is evidence, not certainty',paragraphs:['Xroga can run applicable checks and report their output. Passing tests reduce known risk; they do not prove the absence of vulnerabilities or operational failures. High-risk changes still require appropriate review.']},{heading:'Deployment truth',paragraphs:['A production status requires evidence from the authorised deployment provider and verification of the running result. Missing environment values, DNS, quotas, or permissions remain explicit blockers.']},{heading:'Responsible reporting',paragraphs:['Security concerns can be sent through the contact channel. Do not include passwords, tokens, private keys, sensitive customer data, or exploitable production details in public community posts.']} ]}/>}

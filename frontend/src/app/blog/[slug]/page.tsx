import {notFound} from 'next/navigation';import {EditorialPage} from '@/components/seo/EditorialPage';import {BLOG_ARTICLES} from '@/lib/seoGrowthContent';import {buildMetadata} from '@/lib/seo';export function generateStaticParams(){return BLOG_ARTICLES.map(({slug})=>({slug}));}export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=BLOG_ARTICLES.find(x=>x.slug===slug);return p?buildMetadata({title:p.title,description:p.description,path:`/blog/${slug}`}):{};}export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=BLOG_ARTICLES.find(x=>x.slug===slug);if(!p)notFound();return <EditorialPage path={`/blog/${slug}`} title={p.title} description={p.description} eyebrow={p.eyebrow} intro={p.intro} article updated={p.updated ?? "2026-09-09"} breadcrumbs={[{label:'Guides',href:'/blog'},{label:p.title,href:`/blog/${slug}`}]} related={p.related} sources={p.sources} sections={p.sections}/>}

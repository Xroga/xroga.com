import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Pricing — Xroga AI',
};

export default function PricingLayout({ children }: { children: React.ReactNode }) {
  return children;
}

import { EditorialHub } from '@/components/seo/EditorialPage';import { ALTERNATIVES } from '@/lib/seoGrowthContent';import { buildMetadata } from '@/lib/seo';
export const metadata=buildMetadata({title:'AI Builder and Coding Tool Alternatives',description:'Find legitimate alternatives by working model: browser builder, cloud IDE, AI code editor, or repository-centred delivery.',path:'/alternatives'});
export default function Page(){return <EditorialHub path="/alternatives" title="Choose an alternative by the job it must replace." description={metadata.description as string} eyebrow="Alternatives without affiliate rankings" intro="A product can be an excellent tool and still be the wrong fit for a particular codebase, team, or release process. These shortlists explain the category trade-offs and link to deeper comparisons." items={ALTERNATIVES.map(item=>({href:`/alternatives/${item.slug}`,title:`${item.competitor} alternatives`,description:item.intro}))}/>}

import {notFound} from 'next/navigation';import {EditorialPage} from '@/components/seo/EditorialPage';import {ALTERNATIVES} from '@/lib/seoGrowthContent';import {buildMetadata} from '@/lib/seo';
export function generateStaticParams(){return ALTERNATIVES.map(({slug})=>({slug}));}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=ALTERNATIVES.find(x=>x.slug===slug);return p?buildMetadata({title:`${p.competitor} alternatives for different software workflows`,description:p.intro,path:`/alternatives/${slug}`}):{};}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=ALTERNATIVES.find(x=>x.slug===slug);if(!p)notFound();return <EditorialPage path={`/alternatives/${slug}`} title={`${p.competitor} alternatives for different software workflows`} description={p.intro} eyebrow="Alternatives guide" intro={p.intro} note="This is a category map, not a paid or universal ranking. Verify current capabilities and pricing on each provider’s official site." breadcrumbs={[{label:'Alternatives',href:'/alternatives'},{label:`${p.competitor} alternatives`,href:`/alternatives/${slug}`}]} related={[p.compare,'/compare','/blog/best-vibe-coding-tools']} sections={[{heading:'A practical shortlist',paragraphs:['Each option below represents a distinct working model.'],bullets:p.choices},{heading:'Start with the capability you are replacing',paragraphs:['Write down whether the requirement is code editing, a cloud runtime, browser generation, source-control workflow, hosting, or end-to-end delivery. A long feature checklist cannot compensate for a mismatch in the primary working model.']},{heading:'Evaluate with your own project',paragraphs:['Use a bounded task with an observable acceptance condition. Review repository access, branch behaviour, changed files, tests, deployment evidence, usage, and the recovery path after failure.']},{heading:'Keep current facts current',paragraphs:['Product features and plans change. Follow the linked comparison to see the official sources used for the factual assessment, and verify them again before purchasing.']} ]}/>}

import {notFound} from 'next/navigation';import {EditorialPage} from '@/components/seo/EditorialPage';import {INTEGRATION_GUIDES} from '@/lib/seoGrowthContent';import {INTEGRATION_PHASE2} from '@/lib/seoPhase2Content';import {buildMetadata} from '@/lib/seo';export function generateStaticParams(){return INTEGRATION_GUIDES.map(({slug})=>({slug}));}export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=INTEGRATION_GUIDES.find(x=>x.slug===slug);return p?buildMetadata({title:p.title,description:p.description,path:`/build-with/${slug}`}):{};}export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=INTEGRATION_GUIDES.find(x=>x.slug===slug);if(!p)notFound();const detail=INTEGRATION_PHASE2[p.slug];return <EditorialPage path={`/build-with/${slug}`} title={p.title} description={p.description} eyebrow={p.eyebrow} intro={p.intro} note="Connection availability depends on the user’s account, permissions, provider status, and Xroga’s current supported operation. The interface must not imply a provider action succeeded without evidence." breadcrumbs={[{label:'Build with',href:'/build-with'},{label:p.title,href:`/build-with/${slug}`}]} related={detail?.related ?? p.related} sources={detail?.sources} sections={[...p.sections,...(detail?.sections ?? [])]}/>}

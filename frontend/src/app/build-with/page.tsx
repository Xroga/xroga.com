import {EditorialHub} from '@/components/seo/EditorialPage';import {INTEGRATION_GUIDES} from '@/lib/seoGrowthContent';import {buildMetadata} from '@/lib/seo';export const metadata=buildMetadata({title:'Build With Connected Software Providers',description:'Implementation guides for using Xroga with GitHub, Vercel, Supabase, Whop, Cloudflare, and Brevo through authorised accounts.',path:'/build-with'});export default function Page(){return <EditorialHub path="/build-with" title="Connect providers without hiding the boundary." description={metadata.description as string} eyebrow="Provider-aware build guides" intro="A real integration includes permissions, protected secrets, failure states, and evidence. These guides explain what belongs in the repository and what must be configured in an authorised provider account." items={INTEGRATION_GUIDES.map(x=>({href:`/build-with/${x.slug}`,title:x.title,description:x.description}))}/>}

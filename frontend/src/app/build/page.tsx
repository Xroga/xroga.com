import {EditorialHub} from '@/components/seo/EditorialPage';import {BUILD_GUIDES} from '@/lib/seoGrowthContent';import {buildMetadata} from '@/lib/seo';
export const metadata=buildMetadata({title:'What You Can Build With Xroga AI',description:'Production-minded build guides for SaaS apps, CRMs, booking products, dashboards, internal tools, landing pages, property platforms, and fitness trackers.',path:'/build'});
export default function Page(){return <EditorialHub path="/build" title="Build the product—not only the first screen." description={metadata.description as string} eyebrow="Production-minded build library" intro="These guides connect a product idea to the data, permission, validation, and operating decisions that make it maintainable. They describe supported work patterns without pretending every provider or release is automatic." items={BUILD_GUIDES.map(x=>({href:`/build/${x.slug}`,title:x.title,description:x.description}))}/>}

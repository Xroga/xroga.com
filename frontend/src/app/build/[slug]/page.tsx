import {notFound} from 'next/navigation';import {EditorialPage} from '@/components/seo/EditorialPage';import {BUILD_GUIDES} from '@/lib/seoGrowthContent';import {buildMetadata} from '@/lib/seo';
export function generateStaticParams(){return BUILD_GUIDES.map(({slug})=>({slug}));}export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=BUILD_GUIDES.find(x=>x.slug===slug);return p?buildMetadata({title:p.title,description:p.description,path:`/build/${slug}`}):{};}export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=BUILD_GUIDES.find(x=>x.slug===slug);if(!p)notFound();return <EditorialPage path={`/build/${slug}`} title={p.title} description={p.description} eyebrow={p.eyebrow} intro={p.intro} breadcrumbs={[{label:'Build guides',href:'/build'},{label:p.title,href:`/build/${slug}`}]} related={p.related} sections={[...p.sections,{heading:'A useful prompt to start with',paragraphs:[`Describe the users, core workflow, required data, integrations, constraints, and acceptance checks. Ask Xroga to inspect the current project before changing it and to report the build, test, and deployment evidence separately.`]},{heading:'Before calling it production-ready',paragraphs:['Review permissions, data handling, error states, accessibility, mobile behaviour, tests, environment configuration, observability, and rollback. The exact checklist must follow the project rather than a fixed framework template.']} ]}/>}

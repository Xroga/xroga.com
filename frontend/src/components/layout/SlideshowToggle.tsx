'use client';

/**
 * Wallpaper slideshow was removed with the Deep Work / image theme.
 * Kept as a no-op export so any leftover imports stay harmless.
 */
export function SlideshowToggle() {
  return null;
}

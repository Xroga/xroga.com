# Search provider adapters

Status: **Implemented**.

Adapters expose query, bounded result count/content, attribution, freshness, cancellation and normalized failure behavior. Provider selection can fail over without merging incompatible evidence. Deterministic record/replay fixtures allow CI without consuming paid quotas.

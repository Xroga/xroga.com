# Domain Autopilot

Status: **Implemented for supported providers**; DNS writes and purchases are **external setup required**.

The runtime detects supported ownership paths, captures DNS snapshots, preserves unrelated/protected records, presents conflicts, requires authorization, applies bounded changes, verifies DNS/SSL/product routing and retains rollback evidence. Domain purchase always requires an explicit owner confirmation.

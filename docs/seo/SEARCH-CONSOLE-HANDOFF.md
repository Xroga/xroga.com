# Google Search Console handoff

Search Console was not available to this implementation environment, so no URL inspection or sitemap submission is claimed.

Owner actions:

1. Open the verified `xroga.com` domain property.
2. Submit `https://xroga.com/sitemap.xml`.
3. Inspect `/`, `/ai-app-builder`, `/ai-coding-agent`, `/features/ai-chat`, `/vibe-coding`, the three priority blog articles, the strongest comparison, alternative, Build, and Build-With pages.
4. Confirm `/features/ai-chat` returns 200, is indexable, and declares `https://xroga.com/features/ai-chat` as its canonical. Do not request a redirect to `/ai-coding-agent`.
5. Request indexing only after the live checks pass.
6. Review page indexing, Core Web Vitals, queries, countries, devices, CTR, and rich-result enhancement reports over time.

Organic traffic is not immediate or guaranteed. Use Search Console evidence to improve existing pages before creating more URLs.

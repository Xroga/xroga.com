# SEO baseline — September 11, 2026

Status: repository and production-crawl baseline. Search Console performance data is unavailable because the authorised connection requires reauthentication. No impressions, clicks, rankings, indexed-URL counts, AI citations, or conversion values are estimated.

## Verified technical baseline

- Canonical origin: `https://xroga.com`.
- Framework: Next.js 15.5 App Router.
- Public routes use server-rendered titles, descriptions, self-canonicals, Open Graph and Twitter metadata.
- Private workspace, account, API, preview, and terminal surfaces are excluded from the sitemap and blocked or marked `noindex`.
- `robots.txt`, `sitemap.xml`, `llms.txt`, the manifest, favicon, and Open Graph image are first-party routes/assets.
- The automated audit contract is `scripts/seo-contracts.json`; the live crawler is `scripts/seo-audit.mjs`.
- Microsoft Clarity and the configured Google tag load after interaction through Next.js Script.

## Measurement baseline

| Evidence | Status | Next action |
|---|---|---|
| Google Search Console query/page/device/country data | unavailable | Reauthenticate the existing GSC connection and export a 28-day and 90-day comparison. |
| Google index coverage and URL inspection | unavailable | Verify the domain property and inspect priority canonical URLs. |
| Bing Webmaster Tools / IndexNow reporting | unavailable | Verify the domain and configure an IndexNow key before submission. |
| GA4 acquisition and conversions | not read in this task | Confirm the configured property and define activation events without personal data. |
| Clarity qualitative behavior | script present; dashboard not read | Review rage clicks, dead clicks, scroll depth, and recordings after traffic is available. |

The first valid reporting window begins after verified analytics and webmaster-tool access, not from this file’s date.

## Truth baseline

Xroga is described as an AI app builder and coding agent that can work from a prompt or existing repository, make reviewable source changes, run applicable checks, and help publish through authorised accounts. Current public plans are Free at $0 with no card and Xroga Pro at $25/month. Rankings, traffic, deployment, and provider availability are never guaranteed.

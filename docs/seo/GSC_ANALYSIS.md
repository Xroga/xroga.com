# Google Search Console analysis

Status: **NEEDS SEARCH CONSOLE**.

No authorised Search Console performance or indexation dataset was available during implementation, so no query, click, impression, CTR, average-position, coverage, or Core Web Vitals result is claimed.

When access is available, export 16 months of page/query/device/country data; separate brand from non-brand; map queries to canonical clusters; inspect sitemap discovery and priority URL status; then prioritise pages with impressions in positions 4–20 or high impressions with weak CTR. Preserve privacy and do not publish raw user-sensitive data.

# User-owned integrations

Status: **Implemented contracts and deterministic fixtures**; **external setup required** for live use.

Credentials remain owner-supplied, server-side and least-privileged. Xroga records required environment names, credential status, authenticated probe requirements, rollback and provider evidence. Secrets, private keys and seed phrases are excluded from product manifests, model context and normal logs.

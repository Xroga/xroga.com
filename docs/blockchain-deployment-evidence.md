# Blockchain deployment evidence

Status: **Implemented manifest validator**; public deployment is **external setup required**.

A valid deployment record includes repository commit, family, network and chain ID, toolchain/compiler, source and interface hashes, initialization hash, deployer, transaction hash, address/program ID, block/slot, authorities, environment and evidence path. Verified source additionally requires a real verification URL. No address, receipt or explorer URL was generated for Command 2C because no chain transaction was authorized.

# Incidents

Incident records use open → acknowledged → resolved states, severity, evidence IDs, actor, and timestamps. Provider/readiness failures create blockers; they do not fabricate incident resolution. Operational tables are server-only and changes are auditable.

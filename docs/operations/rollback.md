# Rollback

Rollback requires a previous verified release and backward-compatible schema. Missing targets or incompatible migrations block execution. Fly workflow concurrency no longer cancels an active production release. No live rollback was attempted during Command 3A.

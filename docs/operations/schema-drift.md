# Schema drift

Current live schema drift is `unknown`: no read-only schema comparison was available in this run. The migration workflow proves application attempts, not full schema equality. A future verified check must compare migration history and expected tables without exposing schema secrets.

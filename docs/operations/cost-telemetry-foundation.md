# Cost telemetry foundation

Cost evidence should key usage by release, run, provider, model, task class, token usage, and estimated cost without storing prompts or secrets. Existing model routing outcomes supply part of this data. Provider billing reconciliation and budgets are not verified in Command 3A and remain `unknown`.

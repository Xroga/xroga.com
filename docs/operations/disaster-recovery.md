# Disaster recovery

Recovery order: declare incident → freeze promotion → preserve evidence → select verified release/backup → restore into isolation → validate critical workflows → approve traffic change → monitor → close with evidence. Production destructive steps always require explicit authorization.

# Missing inventory — Xroga ship completeness

Branch: `cursor/ship-completeness-a9a2`

## Already strong (kept)
- Web: Converter → build → sticky GitHub → Vercel
- Research honesty / token honesty / ship preflight
- Expo scaffold + one-click EAS trigger on **user** Expo token
- Crypto/agent packs with custody/trading disclaimers

## Implemented this pass

### Step 1 — Dead / oversold claims
- [x] Pruned `features.ts`, capabilities reply, quick actions, feature catalog, action costs
- [x] Rewrote SEO image page + FAQ/llms honesty
- [x] Softened creationIntelligence follow-ups that promised image/video studios

### Step 2 — Harden Next.js ship
- [x] Vercel Next/Vite deploys no longer silently fall back to static on failure

### Step 3 — Chrome extension loop
- [x] `chrome` scaffold kind (MV3 + popup + service worker + `npm run zip`)
- [x] In-memory `packageBuildZip` + tests
- [x] Preview-only Vercel deploy (story `index.html`)
- [x] Post-build tips for sideload / CWS (~$5 user-paid)

### Step 4 — Desktop loop (free path first)
- [x] `electron` scaffold + GitHub Releases workflow
- [x] PUBLISH.md documents unsigned free path vs user-paid signing

### Step 5 — Expo connect UX + FAQ
- [x] FAQ + llms.txt match in-app one-click EAS (not “on your machine”)
- [x] Publish panel: **Connect Expo** language + fee handoff clarity
- [x] Checklist copy updated server-side

## Explicitly NOT free (documented only)
- Apple ~$99/yr, Google Play ~$25, EAS minutes, CWS ~$5, code signing
- No Expo CLIENT_ID/SECRET on Xroga — user robot/access token only

## Still out of scope
- Managed always-on bot farm
- True Expo OAuth (Expo has no GitHub-like Authorize for CI)
- Paying store fees for users
- Tauri as a separate scaffold (Electron covers free desktop path first)
